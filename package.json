{
  "name": "openclaw-workspace-dispatcher",
  "version": "1.0.0",
  "description": "OpenClaw Dispatcher Agent Workspace",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "playwright": "^1.59.1",
    "redis": "^5.12.1",
    "sqlite3": "^6.0.1"
  }
}
