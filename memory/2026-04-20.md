# 2026-04-20

- 排查 Feishu 会话里出现的 `⚠️ 🔌 Gateway failed` 假故障提示。
- 日志确认真实触发点是 `gateway config.patch` 的可恢复冲突：`config changed since last load; re-run config.get and retry`，不是 Gateway daemon 宕机。
- 已在全局安装的 OpenClaw 运行时代码中打两层补丁：
  1. `pi-embedded-runner-DN0VbqlW.js`：对 `gateway` 工具的可恢复 config hash conflict 不再追加用户可见 warning。
  2. `openclaw-tools-CUmYpN1l.js`：将 `config.patch` / `config.apply` 的这类冲突改写为更准确的人话，明确说明这是配置基线变化，不是 Gateway 挂掉。
- 两次补丁后均完成语法检查并通过 `gateway.restart` 热重启生效。
- `openclaw doctor --non-interactive` 结果：无新的关键异常；仅看到当前主 session lock 正常存在（活跃、非 stale）。
- 现状：这类并发配置冲突以后不应再在 Feishu 中显示成含糊的 `Gateway failed`。
