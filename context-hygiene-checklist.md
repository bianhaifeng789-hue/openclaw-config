# context-hygiene-checklist.md

OpenClaw 上下文卫生最小检查清单。

## 目标
在不依赖模型自动压缩的情况下，主动控制主线程体积和噪音。

## 检查项

- 当前线程是否同时承载：诊断 + 修复 + 验证 + 复盘？
- 是否已有长日志停留在主线程？
- 是否已经有可落到文件的摘要，却还留在聊天里？
- 当前问题是否已进入实现细节而非用户决策？
- 是否适合 closeout 后转独立线程？

## 触发动作

满足任一项时，优先执行：
1. 写摘要到文件
2. 在主线程只保留结论
3. 必要时切到独立线程继续
