# 2026-04-24

- 用户要求先执行可逆清理，已运行 `openclaw doctor --fix --non-interactive`。
- Doctor 安全归档了 `~/.openclaw/agents/main/sessions` 下 30 个 orphan transcript 文件，格式为 `.deleted.<timestamp>` 备份；复查后 Gateway / Feishu / 主 session / fallback 模型链都正常。
- 今日已把默认模型链确认并落地为：`openai/gpt-5.4` → `openai_balance/gpt-5.4` → `bailian/glm-5`。
- 继续排查 `openclaw tasks audit` 中 144 个 `inconsistent_timestamps` warning，确认主要模式是 `startedAt is earlier than createdAt`，差值仅 1–2ms，集中在 heartbeat / cron 任务，判断为低风险时间戳写入顺序瑕疵而非任务损坏。
- 按用户要求走修复路线 B：先在工作区实现 `impl/utils/background-task-service.ts` 中补齐 `createdAt` 字段，并让 `createdAt` / `startedAt` 复用同一个 `now`，避免再制造新的毫秒级倒挂。
- 进一步追到生产路径：`openclaw tasks audit` 走的是全局安装包里的 `dist/task-registry-BdqH6Lnx.js`。其中 `createTaskRecord()` 会把 `createdAt` 强制写成新的 `Date.now()`，而上游多处创建运行中任务时已经先传入 `startedAt`（已确认至少包括 `server.impl-DLF59fRo.js` 的 cron / cli、`acp-spawn-CMKRw5Zj.js` 的 ACP、`openclaw-tools-CxKgYaee.js` 的 media generation、`subagent-registry-CIwqa9KV.js` 的 subagent），因此这 144 个 warning 的根因已坐实在生产代码路径上。
- 当前本地补丁还没有覆盖全局安装包；如果继续修，就该改生产版 `createTaskRecord()`，让 `createdAt` 优先复用 `params.startedAt` 或共享同一个 `now`。
- 随后已直接热补丁全局安装包 `~/.npm-global/lib/node_modules/openclaw/dist/task-registry-BdqH6Lnx.js`：在 `createTaskRecord()` 中新增 `const createdAt = typeof params.startedAt === "number" ? params.startedAt : now;`，并让 record 使用 `createdAt`。我也确认补丁文本已在目标文件落下。注意：历史 warning 仍会保留，因为审计扫的是已有旧记录；这次修复的价值在于阻止同类新记录继续增长。
- 同日继续完成收口：再次运行 `openclaw doctor --fix --non-interactive`，又安全归档了 10 个 orphan transcript；随后确认当前 2026.4.22 实际运行文件已变为 `dist/task-registry-BJCE3lhL.js`，旧 hash 补丁不会自动跟随版本漂移。
- 已直接热补当前真实运行文件，把 `createdAt: now` 改为 `createdAt: params.startedAt ?? now`；重启 gateway 后，创建新的最小验证任务并复查 `openclaw tasks audit`，warning 总数维持 149、未继续增长，说明新增 `inconsistent_timestamps` 已止住。
- 为避免后续升级/重装覆盖热补丁，又在 workspace 增加了持久补丁机制：新增 `scripts/openclaw-patches/task-createdat-startedat-alignment.patchspec.json`，并在 `package.json` 增加 `patch:openclaw:task-timestamps` / `patch:openclaw:task-timestamps:check`；dry-run 已验证能匹配当前安装文件并识别为 already patched。相关变更已提交 git：`8b3fe63` (`Add durable OpenClaw task timestamp patch`)。
